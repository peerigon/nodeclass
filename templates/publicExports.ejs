<% _($thisProperties.public.attribute).each(function printPublicSetterGetter($value, $key) { %>
    <% var $setterKey = $nameModifiers.setter($key); %>
    <% if ($thisProperties.public.method[$setterKey] === undefined) { %>
this.<%= $setterKey %> = <%= $generate.setter($key) %>;
    <% } %>

    <% var $getterKey = $nameModifiers.getter($key); %>
    <% if ($thisProperties.public.method[$getterKey] === undefined) { %>
this.<%= $getterKey %> = <%= $generate.getter($key) %>;
    <% } %>
<% }); %>

<% _($thisProperties.public.method).each(function printPublicMethods($value, $key) { %>
this.<%= $key %> = properties.<%= $key %>;
<% }); %>