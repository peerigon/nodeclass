<% /* this function can be accessed within the init-function via this.Super(); */ %>
Properties.Super = function () {
    Properties.Super = Class.Extends.$construct(Instance, arguments, implAbstracts);

    return Properties.Super;
};