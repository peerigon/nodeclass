<% /*

    this function can be accessed within the init-function via this.Super();

 */ %>
instance.Super = function () {
    var superFlagObj = Class.Extends["@"];

    superFlagObj.ChildClass = Class;
    superInstance = instance.Super = superFlagObj.construct(arguments);
};