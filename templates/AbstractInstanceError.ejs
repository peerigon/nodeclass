<% /* this error gets thrown if the class is abstract and the constructor is not called by a child class */ %>
if(!exposeProtected && !exposeNothing) {
    throw new Error("class error in <%= classFile %>: you cant instantiate an abstract class.\n"
        + "these methods are declared as abstract: <%= abstractMethodNames %>.");
}