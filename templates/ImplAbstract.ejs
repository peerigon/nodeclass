<% /*

    assigns an abstract methods that is implemented by a child class to the current instance-object.
    this is necessary so you can call this.abstractMethod in an abstract class.

*/ %>

instance.<%= key %> = childInstance.<%= key %>;