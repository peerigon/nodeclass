var Module = this;
var Class = module.exports; <% /* we don't know the variable name of the Class-descriptor but the object should be
exported */ %>
var Extends = Class.Extends;

<%= classConstructor %>

<%= statics %>

if (Class.Extends) {
    Constructor.Extends = Class.Extends;
}

if (typeof <%= className %> !== "undefined") {
    <%= className %> = Constructor;
}

module.exports = Constructor;